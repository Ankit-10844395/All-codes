1> launch an ec2 instance a normal one 8g , gp2


	Aws-> cloud formation
	Azure -> ARM Template
	GCP -> Google deployment
	Private cloud -> Openstack -> Heat Template


terraform -> was developed my Michael Haysey , developed in Go-lang

IAC-(Infrastructure as a code)

- INSTALL terraform from opensource terraform 

sudo dnf install -y dnf-plugins-core


terraform -v
mkdir /terra-code
aws configure
share access key and secret key 


let say u have three boxes 
1> red
2> yellow
3> both 

.lets say u have infinite boxes in how many steps can u separate the balls

































--------------------------------------------------------------------------------------------------------------------------------------------

dig google.com
ping google.com
commands from google
create an iam user - admin access
access key
 
terraform -v
mkdir /terra-code
aws configure
 
cd /terra-code/
vim provider.tf
provider "aws" {
  region     = "us-west-2"
  access_key = "AKIAIOSFODNN7EXAMPLE"
  secret_key = "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"
}
 
resource "aws_instance" "dev-server" {
  ami = 
  instance_type = t3.micro
tags = {
  Name = "helloworld"
  }
}
 
 
AMI id copy from 
terraform init - will fetch respective plugins from terraform.io
ls -a 
tree -a
yum install tree -y
tree -a
terraform fmt
terraform validate
terraform plan
terraform apply
 
cat terraform.tfstate - this will will contain all actual resources of your file.
 
terraform destroy
 
mkdir /data-code
vim .tf
provider "aws" {}
 
ll
cd /data-code
vim resource.tf
provider "aws {}
 
resource "aws_instance" "dev-server" {
  ami           = ""
  instance_type = "t3.micro"
  availability_zone = "us-east-1"
  key_name = ""
  tags = {
    Name = "HelloWorld"
  }
}
 
terraform init
terraform fmt
terraform validate
terraform apply
cd
 
ls -a
exit 
aws configure
 
 
copy code for aws_security_group
 
ll
cd /
ll
cd /data-code
ll
mv terraform.tfstate /tmp/
ll
vim resource.tf


resource .tf -> name of file that gets created when we do cat terraform.tfstate


